<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'Flash Sale', root_url, class: "navbar-brand" %>
    </div>
    <div class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li><%= link_to t('my_cart') , line_items_path %></li>
        <li><%= link_to t('past_deals'), root_url(past_deals: true) %></li>

        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <strong id="user_name">
              <%= current_user.first_name.capitalize %> <span class="caret"></span>
            </strong>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to t('my_orders'), orders_path %></li>
            <li><%= link_to t('logout'), logout_path %></li>
            <% if current_user.admin? %>
              <li><%= link_to t('administration'), admins_root_path %></li>
            <% end %>
          </ul>
        </li>
      <% else %>
        <li><%= link_to t('signup'), signup_path %></li>
        <li><%= link_to t('login'), login_path %></li>
      <% end %>
    </div>
  </div>
</nav>
